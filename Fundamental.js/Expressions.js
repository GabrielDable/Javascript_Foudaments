/* Expressions.
Expression is phrase that can be evaluated to produce a value.
A Statement alter the program state, differently of phrases that only produces values.
Functions are expressions
Every time an expression is evaluated a value is produced.
Funtions Expressions when evaluated produces a Function. */

